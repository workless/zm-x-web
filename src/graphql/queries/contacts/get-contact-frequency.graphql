query getContactFrequency($email: String!, $by: String!, $offsetInMinutes: Int) {
	getContactFrequency(email: $email, by: $by, offsetInMinutes: $offsetInMinutes) {
		data {
			by
			dataPoint {
				label
				value
			}
		}
	}
}
